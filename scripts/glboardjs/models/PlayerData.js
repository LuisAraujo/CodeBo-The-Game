export default class PlayerData {
  constructor(day_birthday, gender, id, name) {
    (this.day_birthday = day_birthday),
      (this.gender = gender),
      (this.id = id),
      (this.name = name);
  }
}
